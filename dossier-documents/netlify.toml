[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback (une seule page React).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"
